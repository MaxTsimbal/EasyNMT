# Install Task 3C.1.1

Copy the archive over the existing project while preserving `.git`, `.env`, `.venv`, and `instance`.
Run the full test suite before committing and deploying.
No database migration command is required.
